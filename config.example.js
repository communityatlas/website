// Example configuration file
// You can copy this to config.js and modify the URLs as needed
// Or use the Settings page in the web interface

const exampleConfig = {
    // Kumu website URL (external website)
    kumuUrl: 'https://kumu.io',
    
    // Local Laama instance URL
    // Common localhost ports: 3000, 8000, 8080, 5000
    laamaUrl: 'http://localhost:3000',
    
    // Split ratio (percentage for Kumu side, 20-80)
    splitRatio: 50
};

// Note: The actual configuration is stored in localStorage
// and can be changed via the Settings page in the web interface







